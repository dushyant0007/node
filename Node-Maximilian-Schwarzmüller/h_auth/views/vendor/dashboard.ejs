<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <h1>Hello</h1>

    <form action="/vendor/dashboard/add-new-service" method="post">
        <fieldset style="display: inline;">
            <legend>Add Service</legend>
            <label for="servicType">Select Service Type</label>
            <select id="servicType" name="serviceType">
                <option value="photographer">photographer</option>
                <option value="videographer">videographer</option>
            </select>
            <button type="submit">Add New Service</button>
        </fieldset>
    </form>

    <fieldset style="display: inline;">
        <legend>Your Service</legend>
        <% if (services.length==0){ %>
            <h3>You have no services yet</h3>
            <% } else { %>
                <% for(service of services) { %>
                    <form action="/vendor/dashboard/edit-service/<%= service._id %>" method="get">
                        <label for="servicType">
                            <%= service.serviceType %>
                        </label>
                        <input type="hidden" name="serviceId" value="<%= service._id %>">
                        <button type="submit">Edit Service</button>
                        <a href="/vendor/dashboard/edit-albums/<%= service._id %>">Edit Service Album</a>
                    </form>
                    <% } %>
                        <% } %>
    </fieldset>

</body>

</html>